# Pinned to an exact smart-spatial-system release so results in this
# repository stay reproducible as that project keeps developing. Bump this
# deliberately (and re-run the affected notebooks) - never loosen it to
# ">=" for day-to-day work.
#
# NOTE (2026-09-13): not yet published to PyPI - install directly from the
# tagged commit until it is:
#
#   pip install "smart-spatial-system[raster,pdf] @ git+https://github.com/arazshah/smart_spatial_system.git@v0.2.2"
#
# Switch the line below to the plain PyPI pin once a tagged release is
# published, and delete this note.
#
# 0.2.2 (2026-09-13) fixes the exact bug phase 4 hit twice in this repo
# (two full N=20 batches, both all-degenerate/score=0.0): the LLM prompt
# never taught chaining multiple operations' outputs together before
# score_features, and validation never caught a scorer wired to a stale
# pre-distance vector. Per smart_spatial_system's own CHANGELOG.md,
# `_domain_guidance()` now has a worked "wrong fan-out vs. correct chained"
# example plus `join_feature_properties` guidance, and a new
# `_validate_score_features_field_chaining()` check raises
# LLMSpecGenerationError instead of silently producing an all-zero ranking.
# See claude/smart-spatial-system-upstream-bugs.md (Bug 4) and
# claude/phase-4-llm-arm.md for the full diagnosis this fix responds to.
smart-spatial-system[raster,pdf]==0.2.3

# Notebook environment
jupyterlab>=4.0
ipykernel>=6.29

# Analysis
pandas>=2.2
numpy>=1.26
scipy>=1.13          # spearmanr for the Rank Stability metric

# Mapping / plotting
matplotlib>=3.8
folium>=0.16
geopandas>=0.14

# LLM arm needs nothing beyond what smart-spatial-system already pulls in
# (its own OpenAI-compatible client) - no extra client library required.

python-dotenv>=1.0
